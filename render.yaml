services:
  - type: worker
    name: telegram-html-decrypt-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
      playwright install-deps
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0